# AXE PRODUCT WEB 3.26.3 · ONBOARDING FLOW INTEGRITY

## 목적
첫 접속 → 회사 생성/멤버 확인 → Discord Guided Setup → 대시보드 흐름을 전체 점검하고, 실제 이용 중 혼동이나 중복 생성으로 이어질 수 있는 경계를 보강했습니다.

## 이번에 수정한 항목

### 1. 회사 생성 결과 처리 보강
- `create_company` RPC 반환값을 `{id}` 한 형태로만 가정하지 않습니다.
- UUID scalar / object `id` / object `company_id` / array 반환을 모두 정규화합니다.
- RPC 호출 전 같은 시도의 내부 SLUG를 sessionStorage에 30분간 유지합니다.
- 응답 유실/네트워크 오류 뒤 재시도 시 새 회사를 한 번 더 만들기 전에 같은 SLUG의 기존 생성 결과를 먼저 복구합니다.
- RPC 성공 후에도 실제 `companies` row를 다시 확인한 뒤에만 성공으로 처리합니다.
- 기존의 모호한 `생성된 회사 정보를 받지 못했습니다.` 문구를 제거했습니다.
- 성공: `회사 등록이 완료되었습니다. 이어서 초기설정을 시작합니다.`
- 확인 불가: `회사 등록 상태를 확인하지 못했습니다. 새로고침 후 회사 목록을 확인해 주세요.`

### 2. 초기설정 재개 단계 무결성
- 새로고침/재접속/Discord OAuth 복귀/수동 초기설정 열기의 단계 계산을 하나의 `resolveSetupGuideResumeStep()`로 통합했습니다.
- 저장된 STEP이 앞서 있어도 Discord 연결/카탈로그가 준비되지 않았으면 STEP 1로 되돌립니다.
- 관리자/일반 멤버 Discord 역할 설정이 사라졌으면 STEP 2로 되돌립니다.
- 일반 멤버는 기존 원칙대로 자동 초기설정 재개 대상이 아니며 OWNER만 미완료 Guided Setup을 자동 재개합니다.

### 3. Test Center와 실제 기능 목록 동기화
- 체험판 STEP 3의 기능 카드를 하드코딩하지 않습니다.
- 실제 설정에서 사용하는 `MODULE_ORDER` + `MODULE_UI`를 그대로 사용합니다.
- 현재 `공금 / 총알 / 무법지대 / 개조서 / 핀볼 모집 / 요리 주문 / 자산·계좌 관리`가 실제와 체험판에 동일하게 노출됩니다.
- 이후 모듈을 같은 공용 정의에 추가하면 Test Center도 자동으로 따라갑니다.

### 4. 과거 퇴사/정지 멤버 처리 명확화
- Guided Setup 일괄 등록이 과거 `left/suspended` 이력이 있는 멤버를 조용히 `기존 등록`으로만 표시하지 않습니다.
- 해당 사용자는 자동 재활성화하지 않고 `멤버 관리에서 상태 확인 필요`로 별도 집계합니다.
- 정지/퇴사 이력을 초기설정이 임의로 해제하지 않는 안전 경계는 유지합니다.

## 전체 흐름 점검 결과
현재 고객 흐름의 방향은 유지해도 됩니다.

1. Discord 로그인
2. 선등록된 Discord membership 자동 claim
3. 소속 회사가 있으면 바로 회사 진입
4. 소속 회사가 없으면
   - 새 회사 등록 시작
   - 이미 이용 중인 회사 팀원 → 대표/관리자에게 등록 요청 → 등록 확인 전 진입 차단
5. 신규 회사는 회사명만 입력, SLUG 내부 자동 생성
6. 회사 생성 확인 후 Guided Setup 자동 시작
7. Discord 연결 → 역할 → 기능 → 채널 → 멤버 → 완료
8. 중간 이탈 시 OWNER만 실제 선행 조건에 맞는 단계부터 재개
9. 완료 후 대시보드

회사 검색/임의 합류/합류코드는 첫 접속 흐름에 없습니다.

## 확인은 했지만 이번 WEB 패치에서 DB를 임의 변경하지 않은 항목

### Discord placeholder 중복 방지
현재 애플리케이션은 등록 전에 같은 회사의 `discord_user_id`를 조회해서 중복을 피합니다. 다만 DB에 `(company_id, discord_user_id)` 원자적 UNIQUE 경계가 있는지는 별도 운영 데이터 확인 후 강화하는 편이 안전합니다. 과거 중복 row나 퇴사/재입사 정책이 있을 수 있어 이번 WEB 패치에서 인덱스를 임의 추가하지 않았습니다.

향후 DB hardening 전에는 STAGING 실제 데이터에서 중복 Discord ID와 inactive 이력을 먼저 진단한 뒤 정책을 정합니다.

## 배포 범위
- WEB STAGING only
- 추가 SQL 없음
- BOT 변경 없음
- LIVE BOT/LIVE WEB 변경 없음
