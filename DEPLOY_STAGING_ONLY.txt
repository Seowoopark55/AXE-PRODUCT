AXE PRODUCT WEB 3.23.3 · STAGING ONLY

1. Production/LIVE WEB에 바로 배포하지 않습니다.
2. 3.23.2 질문게시판 DB migration을 이미 적용했다면 이번 UI 패치에는 추가 SQL이 필요하지 않습니다.
3. WEB ZIP은 GitHub AXE PRODUCT WEB STAGING 프로젝트에 전체 덮어쓰기합니다.
4. commit / push 후 Vercel STAGING이 Ready인지 확인합니다.
5. 질문게시판은 AXE PRODUCT DB를 원본으로 사용합니다.
6. 질문게시판 때문에 Discord 포럼 채널을 만들거나 연결하지 않습니다.
7. Discord는 작성자 계정이 연결된 경우 답변완료 DM 알림에만 선택적으로 사용합니다.
8. DM 실패/미연동은 질문 저장·답변완료를 실패시키지 않습니다.
9. LIVE BOT `/home/pyipsw001/axe-bot` 및 PM2 `axe-bot`은 건드리지 않습니다.
10. WEB ZIP을 `apply-axe-product.sh`로 배포하지 않습니다.
