AXE PRODUCT WEB STAGING 3.16.6
SEMANTIC TABLE LAYOUT R1

Scope: WEB STAGING only. No DB / BOT changes.

What changed
- Member status list converted from proportional CSS grid rows to a real HTML table.
- Fund ledger converted to a real HTML table with fixed colgroup widths.
- Asset list converted to a real HTML table.
- Asset return history converted to a real HTML table.
- Account list converted to a real HTML table.
- Desktop tables use table-layout: fixed, shared header/body column axes, vertical centering,
  stable cell padding, nowrap/ellipsis for short fields, and explicit colgroup widths.
- Name/detail cells remain left aligned for readability; status/action cells remain centered;
  fund amount is right aligned.
- Mobile keeps a responsive card-like presentation using data-label attributes.

Intent
- Remove the subtle spacing drift and compressed-text feel caused by independent grid tracks.
- Make header and row alignment deterministic across the whole board.
- Keep empty space evenly distributed by the table column model instead of accumulating between blocks.

Validation
- npm run check: PASS
- integrated UI: 72/72 PASS
- row-density: 7/7 PASS
- balanced-grid/table: 11/11 PASS
- semantic-table: 10/10 PASS
- node syntax check for render.js: PASS

Note
- npm run build was not executed in this packaging environment because node_modules/vite are not installed.
  Source-level and project validation scripts completed successfully.
