AXE PRODUCT WEB STAGING 3.16.7
COMPACT SEMANTIC TABLE R2

Source basis
- Rebuilt from the 3.16.6 semantic-table source, with the functional account regression repaired.
- No DB / BOT changes.

Root cause repaired
- 3.16.6 accidentally removed the accountBadge() helper while renderAccounts() still called it.
  Opening Accounts could therefore throw ReferenceError and stop rendering.
- Fund semantic table was forced to width:100% / max-width:none, so a sparse row stretched across
  the full workspace and created the exact 'empty / collapsed' visual reported in testing.

R2 design
- Keep real HTML tables for deterministic header/body alignment.
- Constrain operational boards to content-sized max widths.
- Fund ledger max width 780px and centered.
- Members 650px, assets 660px, accounts 700px.
- Standard font weights (700/800) and slightly larger cell text to avoid synthesized / squeezed feel.
- Stable 34px headers, 60px rows, 9-14px cell padding.
- Name/detail remain readable; status/action stay centered; fund amount remains right aligned.
- Mobile falls back to the existing labeled-card table presentation.

STAGING WEB ONLY. LIVE BOT / PM2 / DB untouched.
