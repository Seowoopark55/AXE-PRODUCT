AXE PRODUCT WEB STAGING 3.16.8 · CLEAN OPERATIONS TABLE R3

- Rebased the management/table work on the last stable 3.16.5 source instead of stacking more CSS over 3.16.7.
- One 860px operational workspace shared by members, assets, returns, and accounts.
- Same left edge across management tabs; removed per-tab centred widths that made each screen drift.
- Kept real semantic tables, but removed repeated in-cell labels already present in the header.
- Text columns are left aligned; status/actions stay centred.
- Fund keeps its compact table; the item/details column is wider and header alignment matches the body.
- Mobile remains card-like.
- No DB or BOT changes.
